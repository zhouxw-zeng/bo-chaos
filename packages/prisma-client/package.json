{
  "name": "@mono/prisma-client",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "postinstall": "prisma generate",
    "prod": "pnpm prisma migrate dev && pnpm generate"
  },
  "dependencies": {
    "@prisma/client": "6.3.1"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
